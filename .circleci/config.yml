version: 2
jobs:
  build:
    docker:
      # Specify the version you desire here
      - image: jonbaldie/yarn

    working_directory: ~/repo

    steps:
      - checkout

      # node cache
      # - run: cd web/app/themes/spinx && npm install webpack --save-dev
      - run: cd web/app/themes/spinx && /root/.yarn/bin/yarn build:production